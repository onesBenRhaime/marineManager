<form method="post" action="{{ path('app_reglement_delete', {'id': reglement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reglement.id) }}">
    <button class="btn">Delete</button>
</form>
