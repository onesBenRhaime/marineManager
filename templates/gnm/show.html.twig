{% extends 'base.html.twig' %}

{% block title %}Gnm{% endblock %}

{% block body %}
    <h1>Gnm</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ gnm.id }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ gnm.email }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ gnm.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ gnm.prenom }}</td>
            </tr>
            <tr>
                <th>Tel</th>
                <td>{{ gnm.tel }}</td>
            </tr>
            <tr>
                <th>Psw</th>
                <td>{{ gnm.psw }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_gnm_index') }}">back to list</a>

    <a href="{{ path('app_gnm_edit', {'id': gnm.id}) }}">edit</a>

    {{ include('gnm/_delete_form.html.twig') }}
{% endblock %}
